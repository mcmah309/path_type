name: path_type
description: A path type providing a type-safe and cross-platform path abstraction for managing and manipulating file paths, supporting both POSIX and Windows file systems.
version: 1.0.0
repository: https://github.com/mcmah309/path_type

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  rust: ^1.0.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
